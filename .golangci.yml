# golangci-lint v2 formati (action@v8 / binary v2.x).
version: "2"

linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - gosec
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
  settings:
    revive:
      rules:
        # Turkce godoc stili ("X, ... yapar") virgul nedeniyle exported
        # kuralinin bicim denetimine takiliyor; ekip stili korunuyor.
        - name: exported
          disabled: true
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
    paths:
      - deployments
      - migrations

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - deployments
      - migrations
